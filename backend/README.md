# Backend for Smart Settlement & Reconciliation System

This folder contains the backend Express app. Follow the project blueprint in `PROJECT_INSTRUCTIONS.md`.

To run locally:

```powershell
cd "d:\Reconciliation System\backend"
npm install
cp .env.example .env # or manually create .env with MONGO_URI and JWT_SECRET
npm run dev
```
